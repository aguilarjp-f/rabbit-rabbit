//* ROAS Colour
IF [ROAS] < 1 
  THEN "Loss"
ELSEIF [ROAS] >= 1 AND [ROAS] < 1.25 
  THEN "Breakeven"
ELSEIF [ROAS] >= 1.25 AND [ROAS] < 2 
  THEN "Slight Profit"
ELSEIF [ROAS] >= 2 AND [ROAS] < 3 
  THEN "Decent Profit"
ELSEIF [ROAS] >= 3 AND [ROAS] < 4 
  THEN "Strong Profit"
ELSEIF [ROAS] >= 4 AND [ROAS] < 5 
  THEN "High Profit"
ELSEIF [ROAS] >= 5 
  THEN "Amazing"
ELSE 
  "Error"
END
